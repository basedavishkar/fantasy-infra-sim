# Terraform Structure (WIP)

Early sketch of how we'd split infra:

- `networking/` — VPC, subnets, gateway, SGs
- `services/` — ECS tasks, service discovery
- `storage/` — RDS, S3, Redis (maybe)

not finalized. placeholder for now.